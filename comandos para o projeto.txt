// Pode agrupar comandos com ';' em uma linha

======================== ABRIR UM APP EXISTENTE ========================

//Logar no heroku

// configurar user git
git config --global user.email "lukaspellezario@hotmail.com"
git config --global user.name "Lucas e Matheus"

// Iniciar git localmente
git init

// Conectar o git ao heroku
heroku git:remote -a <nomedoapp>

// adicionar/Fazer update no heroku
git add *
git commit -m "A comment"
git push heroku master

// abrir aplicação
heroku open

